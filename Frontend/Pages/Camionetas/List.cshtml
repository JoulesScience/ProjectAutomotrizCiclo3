@page
@model MyApp.Namespace.ListModel5
@{
}
<div class= "justify-conten-center text-center">
<h1>Listado de Camionetas</h1>
<br>
</div>
<div>
    <div>
      <a asp-page="Create" class="btn btn-success"> Crear una nueva camioneta</a>
      <i class="fa fa-plus"></i>
    </div>
    <br>
    <table class="table table-dark table-striped">
        <tr>
            <th>Id</th>
            <th>Platon</th>            
            <th>Transmision</th>
            <th>placa_id</th>
            <th>Detalle</th>
            <th>Eliminar</th>
            <th>Actulizar</th>
        </tr>
         @foreach (var camioneta in Model.Camionetas)
          {
            <tr><td>@camioneta.Id</td>
                <td>@camioneta.Platon</td>
                 <td>@camioneta.Transmision</td>
                 <td>@camioneta.placa_id</td>                 
                 <td>
                    <a class= "btn  btn-primary" asp-page="./Details" asp-route-id="@camioneta.Id">
                    <i class="fa fa-circle"></i></a>
                    
                 </td>
                 <td>
                    <a class= "btn  btn-danger" asp-page="./Delete" asp-route-id="@camioneta.Id">
                    <i class="fa-sharp fa-solid fa-trash"></i></a>
                    
                 </td>
                  <td>
                    <a class= "btn  btn-primary" asp-page="./Update" asp-route-id="@camioneta.Id">
                     <i class="fa-sharp fa-solid fa-pencil"></i>
                    </a>
                 </td>
             </tr> 
          }
    </table>
</div>
<div>
    <a asp-page="Index"  class="btn btn-secondary" > Volver</a>
</div>